# Daily Meme Cup

The MVP tournament scheduler is enabled only in the `prod` Spring profile when `app.tournament.enabled=true`. It is designed for a single backend instance and does not use a distributed lock.
