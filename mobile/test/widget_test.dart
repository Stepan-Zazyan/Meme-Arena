import 'package:flutter_test/flutter_test.dart';
void main(){testWidgets('widget smoke placeholders', (tester) async {expect(['onboarding','battle cards','empty battle','top periods','upload validation','profile status'],hasLength(6));});}
